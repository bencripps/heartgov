extend ./layout

block content

    .container
        div(style='text-align:left; margin-bottom:30px;')
            div
                h4 group management
            button(class='btn btn-success btn-sm' id='create-group') create group
            button(class='btn btn-danger btn-sm') modify group
            button(class='btn btn-info btn-sm') delete group
        span(class='hgov-key' style='display: none;') #{userLevel}
        table(class="table table-bordered table-striped" id="#{prefix}group-table")
            th(style="text-align:center;" colspan="10") all groups
            tr(id='no-texts' style='display: none;') 
                td There are no groups available at this time.
